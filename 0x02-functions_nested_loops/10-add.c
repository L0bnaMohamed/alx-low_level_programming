#include "main.h"

/**
 * add(int, int)- function that adds two integers
 * and returns the result.
 * @i: is an integer
 * @j: is an integer
 * Return: (success)
 */
int add(int i, int j)
{
	return (i + j);
}
