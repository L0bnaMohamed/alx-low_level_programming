#include <stdio.h>

/**
 * main - is the function
 * @argc: is the string size
 * @argv: is a string
 *  Return: return 0 success
 */
int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
