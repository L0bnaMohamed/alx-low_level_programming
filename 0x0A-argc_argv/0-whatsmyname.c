#include <stdio.h>

/**
 * main - is the function
 *
 * @argc: is the string size
 *
 * @argv: is a string
 *  Return: return functin
 */
int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n", argv[argc]);
	return (1);
}
