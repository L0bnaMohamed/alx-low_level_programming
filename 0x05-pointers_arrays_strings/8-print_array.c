#include "main.h"

/**
 * print_array(int *a, int n)- is the function
 * @a : is the pointer
 * @n: is a variable of int type
 * Return: return
 */
void print_array(int *a, int n)
{
	int i;
	for (i = 0; i < 5; i++)
	{
		printf("%d", n);
		printf(48);
	}
	_putchar('\n');
}
