#include "main.h"

/**
 * _abs- is a functin to print absulute
 * @i: is an integer
 * Return: Always (success)
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}

}
